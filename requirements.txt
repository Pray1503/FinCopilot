# AI Wealth Coach — Python dependencies
# Install: pip install -r requirements.txt
#
# IMPORTANT: If you previously had google-generativeai installed (old SDK),
# uninstall it first to prevent conflicts:
#   pip uninstall google-generativeai -y
#
# Then install:
#   pip install -r requirements.txt

streamlit>=1.35.0
google-genai>=2.0.0
